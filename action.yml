# action.yml
name: 'Hello World'
description: 'Rust github action POC'
inputs:
  name:  # id of input
    description: 'Name'
    required: true
    default: 'World'
outputs:
  time: # id of output
    description: 'The time we greeted you'
runs:
  using: 'docker'
  image: 'Dockerfile'
